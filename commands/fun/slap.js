const { makeSocial } = require("../../utils/social");

module.exports = makeSocial({ name: "slap", description: "Mets une claque (pour rire) à quelqu'un", endpoint: "slap", text: "met une claque à" });
